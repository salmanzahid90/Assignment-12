//Write a js program to trim both leading and trailing white space characters from given string.
function trimString(str) {
    return str.trim();
  }
  
  let str = "   I M Usama   ";
  console.log(`Trim both sides of the string is ${trimString(str)}`);
  

